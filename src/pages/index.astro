---
const name = "Viktor Belay";
const tagline = "Structural biology • biophysics • computation";
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{name}</title>
    <style>
      :root { color-scheme: dark; }
      body {
        margin: 0;
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
        background: #0b0b0c;
        color: #f2f2f2;
      }
      main {
        max-width: 920px;
        margin: 0 auto;
        padding: 72px 24px;
      }
      nav {
        display: flex;
        gap: 18px;
        margin: 18px 0 44px;
        font-size: 14px;
        opacity: 0.9;
        flex-wrap: wrap;
      }
      a { color: inherit; text-decoration: none; }
      a:hover { text-decoration: underline; }

      h1 {
        font-size: 44px;
        letter-spacing: -0.02em;
        margin: 0;
      }
      .tagline { opacity: 0.75; margin-top: 10px; }

      .hero {
        margin-top: 28px;
        border-radius: 22px;
        height: 220px;
        background:
          radial-gradient(900px 520px at 20% 25%, rgba(255,255,255,0.10), transparent 60%),
          radial-gradient(900px 520px at 80% 35%, rgba(130,170,255,0.12), transparent 60%),
          radial-gradient(900px 520px at 50% 95%, rgba(255,170,120,0.10), transparent 60%),
          rgba(255,255,255,0.03);
        border: 1px solid rgba(255,255,255,0.08);
      }

      p {
        line-height: 1.65;
        max-width: 70ch;
        font-size: 16px;
        opacity: 0.92;
      }

      .section {
        margin-top: 56px;
        padding-top: 20px;
        border-top: 1px solid rgba(255,255,255,0.08);
      }
      .cards { margin-top: 14px; display: grid; gap: 12px; }
      .card {
        border: 1px solid rgba(255,255,255,0.10);
        border-radius: 18px;
        padding: 14px 16px;
        background: rgba(255,255,255,0.02);
      }
      .muted { opacity: 0.7; font-size: 14px; margin-top: 6px; }
    </style>
  </head>
  <body>
    <main>
      <h1>{name}</h1>
      <div class="tagline">{tagline}</div>

      <nav>
        <a href="/research">Research</a>
        <a href="/publications">Publications</a>
        <a href="/software">Software</a>
        <a href="/cv">CV</a>
        <a href="/contact">Contact</a>
      </nav>

      <div class="hero" aria-hidden="true"></div>

      <p style="margin-top: 26px;">
        I study intracellular ion channels and transporters using structure-guided biophysics.
        My work combines cryo-EM, molecular dynamics, and functional assays to turn conformations into mechanism.
      </p>

      <div class="section">
        <div class="muted">Selected projects</div>
        <div class="cards">
          <div class="card">
            <strong>IP3R2 nucleotide potentiation</strong>
            <div class="muted">Structural mechanism of adenine nucleotide modulation of gating.</div>
          </div>
          <div class="card">
            <strong>Endolysosomal CLC transporters</strong>
            <div class="muted">Lipid / nucleotide regulation and domain-state coupling.</div>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>